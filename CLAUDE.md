Use brif style of writing code. No extra fucntions/classes/files.
Stack: taskfile, tailwind, uni_paginator, httpx, Vite
Use `task` command
task migrate must create migrations and do migrate
Custom conftest.py, not pytest-django

Always thinks that app suppose to be used on mobile. Construct it first. Then the rest versions.


When user asks 'complete something', analyze what wrong with the file (pseudocode, syntax, etc). Deeply trace author intention and complete it, not rewrite, not change intenrion but compete!